extends layout

block content
    h1= title
    hr
    form.form-inline(action="/create", method="post", accept-charset="utf-8")
        input.input-medium(type="text", name="title", placeholder="title")
        input.input-medium(type="text", name="artist", placeholder="artist")
        input.input-medium(type="text", name="url", placeholder="url")
        button.btn(type="submit") Submit
    br
    table.table.table-condensed
        thead
            tr
                th
                    p #
                th
                    p Song Name
                th
                    p Artist
                th(style="float:right")
                    p Delete
        tbody
            each song, i in songs
                tr.song(href=song.url)
                    td
                        p #{i + 1}
                    td
                        p #{song.title}
                    td
                        p #{song.artist}
                    td
                        a.btn.btn-danger.btn-mini.delete(href="/destroy/#{song._id}") <i class="icon-remove"></i>
    br
    #track
